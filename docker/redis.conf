# Redis config for container: data dir mounted from host (bases).
# Author: Vasiliy Zdanovskiy
# email: vasilyvz@gmail.com

# 0.0.0.0 so host can connect when port is published (e.g. 63790:6379 for tests)
bind 0.0.0.0
port 6379
dir /app/redis_data
daemonize yes
logfile ""
