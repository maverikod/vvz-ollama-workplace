# redis_provider_client

**Author:** Vasiliy Zdanovskiy  
**email:** vasilyvz@gmail.com

Provider client for **Redis** (storage): клиент к redis-adapter для рабочего места модели. Вызывает redis-adapter по WebSocket; для model_workspace — единый API хранилища. PyPI-publishable.

**Structure:** `docs/` (ТЗ, reports/, plans/), `src/redis_provider_client/`, `tests/` (unit/, integration/), `config/`, `examples/`, `code_analysis/` (generated), `projectid`, `pyproject.toml`, `.gitignore`.

**ТЗ:** `docs/ТЗ.md`. Context: root [SPEC](../../docs/plans/refactoring_adapter_structure/SPEC.md).
