# Rules (example)
# Copy to rules.md and set rules_file_path in config.
# Content is automatically prepended to session rules in model context.

- Answer in one short sentence when possible.
- Use the provided tools to discover and call servers when needed.
