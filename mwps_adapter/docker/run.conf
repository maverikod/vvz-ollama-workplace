# Docker run settings for mwps-adapter.
# Author: Vasiliy Zdanovskiy
# email: vasilyvz@gmail.com

CONTAINER_NAME=mwps-adapter
IMAGE_NAME=mwps-adapter
NETWORK_NAME=smart-assistant

# Optional: CPU-only (e.g. cpu, cpu_avx2). Unset = use GPU if available.
# export MWPS_LLM_LIBRARY=cpu
