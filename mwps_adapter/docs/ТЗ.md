# ТЗ: подпроект mwps_adapter

**Author:** Vasiliy Zdanovskiy  
**email:** vasilyvz@gmail.com

Техническое задание на подпроект Model Workplace Server-адаптера: клиент и сервер на базе mcp-proxy-adapter. Общий контекст — в корне репозитория: `docs/plans/refactoring_adapter_structure/SPEC.md`.

---

## 1. Назначение

- **Сервер mwps-adapter:** Model Workplace Server + сервер API на базе mcp-proxy-adapter; регистрация на MCP Proxy; доступ к Model Workplace Server только через адаптер (WebSocket).
- **Клиент Model Workplace Server на базе адаптера:** передаёт запросы серверу по WebSocket, воспроизводит полный API Model Workplace Server (тоннель); вызывающий код получает питоновские объекты.

---

## 2. Транспорт

- Взаимодействие клиент ↔ сервер — **только WebSocket** (через прокси).

---

## 3. Классы протокола

- **Отдельный класс для запроса/ответа Model Workplace Server** — структура: имя метода/эндпоинта, параметры (model, messages, prompt и т.д.), тело ответа (например, JSON: `method`, `params`, `result` / `error`).
- **Отдельный класс для ошибок Model Workplace Server** — тип для ошибок (HTTP 4xx/5xx, протокол, таймаут); на клиенте — исключение этого класса.

---

## 4. Клиент: один метод + обёртки

- **Базовый метод:** `execute(method_name: str, **params)` — отправляет на сервер имя метода и параметры по WebSocket, получает ответ, десериализует и возвращает питоновские объекты (dict, list, str и т.д.).
- **Остальные методы** — обёртки над `execute()` (tags, chat, generate, embeddings, pull, push, delete, show, copy, create, ps, version). **Валидация параметров** выполняется **в обёртках**, не в `execute()`.

**Полный набор методов (эндпоинты Model Workplace Server):**

| Метод клиента       | Назначение                    |
|---------------------|-------------------------------|
| `tags()` / `list_models()` | Список моделей (GET /api/tags) |
| `chat(model, messages, …)` | Чат (POST /api/chat)          |
| `generate(model, prompt, …)` | Генерация (POST /api/generate) |
| `embeddings(model, input)` | Эмбеддинги (POST /api/embeddings) |
| `pull(model)`       | Скачивание модели             |
| `push(model)`       | Выгрузка модели               |
| `delete(model)`     | Удаление модели               |
| `show(model)`       | Информация о модели           |
| `copy(source, dest)`| Копирование модели            |
| `create(name, modelfile, …)` | Создание из Modelfile    |
| `ps()` / `running()`| Запущенные модели             |
| `version()`         | Версия Model Workplace Server                 |

---

## 5. Сервер mwps-adapter

- Принимает запрос, **десериализует** (имя метода + параметры).
- Вызывает **нативный HTTP-клиент** к локальному Model Workplace Server (соответствующий эндпоинт: /api/tags, /api/chat и т.д.).
- Ответ Model Workplace Server **сериализует в JSON** и отправляет клиенту по WebSocket.

---

## 6. Контейнер

- Контейнер **mwps-adapter**: Model Workplace Server + сервер mcp-proxy-adapter; образ собирается из данного подпроекта.
