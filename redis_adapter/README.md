# redis_adapter

**Author:** Vasiliy Zdanovskiy  
**email:** vasilyvz@gmail.com

Redis adapter subproject: client and server on top of mcp-proxy-adapter (WebSocket, request/response and error classes, single `execute()` + wrappers). PyPI-publishable layout.

**Structure (per project standard):** `docs/` (ТЗ, reports/, plans/), `src/redis_adapter/`, `tests/` (unit/, integration/), `config/`, `examples/`, `code_analysis/` (generated), `projectid`, `pyproject.toml`, `.gitignore`.

**ТЗ подпроекта:** `docs/ТЗ.md`. Общий контекст: корень репозитория `docs/plans/refactoring_adapter_structure/SPEC.md`.
