# model_workspace

**Author:** Vasiliy Zdanovskiy  
**email:** vasilyvz@gmail.com

Model workspace subproject: workstation application using redis_adapter and ollama_adapter clients (no Redis or Ollama inside). PyPI-publishable layout.

**Structure (per project standard):** `docs/` (ТЗ, reports/, plans/), `src/model_workspace/`, `tests/` (unit/, integration/), `config/`, `examples/`, `code_analysis/` (generated), `projectid`, `pyproject.toml`, `.gitignore`.

**ТЗ подпроекта:** `docs/ТЗ.md`. Общий контекст: корень репозитория `docs/plans/refactoring_adapter_structure/SPEC.md`.
